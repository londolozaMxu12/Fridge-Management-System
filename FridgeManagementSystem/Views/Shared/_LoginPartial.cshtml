@using FridgeManagementSystem.Data
@using Microsoft.AspNetCore.Identity
@using Microsoft.EntityFrameworkCore
@using FridgeManagementSystem.Areas.Identity.Data
@using FridgeManagementSystem.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

    @* <!DOCTYPE html >
    <head>
    <style>
        body {
            color: white;
        }
    </style>
    </head>
    <body>
    
    </body>
</html> *@

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        // 🔹 Load full user with Employee + EmployeeType
        var appUser = await UserManager.Users
        .Include(u => u.Employees)
        .ThenInclude(e => e.EmployeeType)
        .FirstOrDefaultAsync(u => u.Id == UserManager.GetUserId(User));
        @* var appUser = await UserManager.GetUserAsync(User); *@

        if (User.IsInRole("Admin"))
        {
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center text-black" href="#" role="button" data-bs-toggle="dropdown">

                        <i class="bi bi-person-circle me-2 fs-5"></i>@(appUser!.FullName + " (Admin)")

                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" asp-area="Admin" asp-controller="Profile" asp-action="Index">
                                <i class="bi bi-person me-2"></i>My Profile
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" asp-area="Admin" asp-controller="Profile" asp-action="Edit">
                                <i class="bi bi-pencil-square me-2"></i>Update Profile
                            </a>
                        </li>

                        <li><hr class="dropdown-divider"></li>
                        <li class="nav-item">

                            <form id="logoutForm" class="dropdown-item text-danger" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                <button id="logout" type="submit" class="nav-link btn btn-link text-danger border-0">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </button>
                            </form>

                        </li>

                    </ul>
                </li>

            </ul>
           
        }
        else if (User.IsInRole("Customer"))
        {
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">

                        <i class="bi bi-person-circle me-2 fs-5"></i>@(appUser!.FullName + " (Customer)")
                       
                    </a>
                    
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" asp-area="Customer" asp-controller="Profile" asp-action="Index">
                                <i class="bi bi-person me-2"></i>My Profile
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" asp-area="Customer" asp-controller="Profile" asp-action="Edit">
                                <i class="bi bi-pencil-square me-2"></i>Update Profile
                            </a>
                        </li>

                        <li><hr class="dropdown-divider"></li>
                        <li class="nav-item">

                            <form id="logoutForm" class="dropdown-item text-danger" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                <button id="logout" type="submit" class="nav-link btn btn-link text-danger border-0">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </button>
                            </form>

                        </li>
                      
                    </ul>
                </li>
            </ul>

        }

        else if (User.IsInRole("Employee") && appUser?.Employees?.EmployeeType?.Name == "FaultTechnician")
        {
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">

                        <i class="bi bi-person-circle me-2 fs-5"></i>@(appUser!.FullName + " (FaultTechnician)")

                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" asp-area="FaultTechnician" asp-controller="Profile" asp-action="Index">
                                <i class="bi bi-person me-2"></i>My Profile
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" asp-area="FaultTechnician" asp-controller="Profile" asp-action="Edit">
                                <i class="bi bi-pencil-square me-2"></i>Update Profile
                            </a>
                        </li>

                        <li><hr class="dropdown-divider"></li>
                        <li class="nav-item">

                            <form id="logoutForm" class="dropdown-item text-danger" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                <button id="logout" type="submit" class="nav-link btn btn-link text-danger border-0">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </button>
                            </form>

                        </li>

                    </ul>
                </li>

            </ul>
        }
        else if (User.IsInRole("Employee") && appUser?.Employees?.EmployeeType?.Name == "MaintenanceTechnician")
        {
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">

                        <i class="bi bi-person-circle me-2 fs-5"></i>@(appUser!.FullName + " (MaintenanceTechnician)")

                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" asp-area="MaintenanceTechnician" asp-controller="Profile" asp-action="Index">
                                <i class="bi bi-person me-2"></i>My Profile
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" asp-area="MaintenanceTechnician" asp-controller="Profile" asp-action="Edit">
                                <i class="bi bi-pencil-square me-2"></i>Update Profile
                            </a>
                        </li>

                        <li><hr class="dropdown-divider"></li>
                        <li class="nav-item">

                            <form id="logoutForm" class="dropdown-item text-danger" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                <button id="logout" type="submit" class="nav-link btn btn-link text-danger border-0">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </button>
                            </form>

                        </li>

                    </ul>
                </li>

            </ul>
        }
        else if (User.IsInRole("Employee") && appUser?.Employees?.EmployeeType?.Name == "InventoryLiaison")
        {
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">

                        <i class="bi bi-person-circle me-2 fs-5"></i>@(appUser!.FullName + " (Inventory Liaison)")

                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" asp-area="InventoryLiaison" asp-controller="Profile" asp-action="Index">
                                <i class="bi bi-person me-2"></i>My Profile
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" asp-area="InventoryLiaison" asp-controller="Profile" asp-action="Edit">
                                <i class="bi bi-pencil-square me-2"></i>Update Profile
                            </a>
                        </li>

                        <li><hr class="dropdown-divider"></li>
                        <li class="nav-item">

                            <form id="logoutForm" class="dropdown-item text-danger" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                <button id="logout" type="submit" class="nav-link btn btn-link text-danger border-0">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </button>
                            </form>

                        </li>

                    </ul>
                </li>

            </ul>
        }
        else if (User.IsInRole("Employee") && appUser?.Employees?.EmployeeType?.Name == "PurchasingManager")
        {
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">

                        <i class="bi bi-person-circle me-2 fs-5"></i>@(appUser!.FullName + " (Purchasing Manager)")

                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" asp-area="PurchasingManager" asp-controller="Profile" asp-action="Index">
                                <i class="bi bi-person me-2"></i>My Profile
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" asp-area="PurchasingManager" asp-controller="Profile" asp-action="Edit">
                                <i class="bi bi-pencil-square me-2"></i>Update Profile
                            </a>
                        </li>

                        <li><hr class="dropdown-divider"></li>
                        <li class="nav-item">

                            <form id="logoutForm" class="dropdown-item text-danger" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                <button id="logout" type="submit" class="nav-link btn btn-link text-danger border-0">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </button>
                            </form>
                        </li>

                    </ul>
                </li>

            </ul>
        }
        else if (User.IsInRole("Employee") && appUser?.Employees?.EmployeeType?.Name == "CustomerLiaison")
        {
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">

                        <i class="bi bi-person-circle me-2 fs-5"></i>@(appUser!.FullName + " (Customer Liaison)")

                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" asp-area="CustomerManagement" asp-controller="Profile" asp-action="Index">
                                <i class="bi bi-person me-2"></i>My Profile
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" asp-area="CustomerManagement" asp-controller="Profile" asp-action="Edit">
                                <i class="bi bi-pencil-square me-2"></i>Update Profile
                            </a>
                        </li>
                        
                        <li><hr class="dropdown-divider"></li>
                        <li class="nav-item">
                            <form id="logoutForm" class="dropdown-item text-danger" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                <button id="logout" type="submit" class="nav-link btn btn-link text-danger border-0">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </button>
                            </form>
                        </li>

                    </ul>
                </li>

            </ul>
        }
        
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-white" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>




