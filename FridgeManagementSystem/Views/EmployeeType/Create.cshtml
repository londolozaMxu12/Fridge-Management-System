
@model EmployeeType
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Create Employee Type";
}

<h1><i class="fas fa-plus"></i> @ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-6">
        <h5 class="mb-0"><i class="fas fa-plus"></i> Create New Employee Type</h5>
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <div class="form-group mb-3">
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save m-1"></i> Create
                </button>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="fas fa-arrow-left m-1"></i> Back to List
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}